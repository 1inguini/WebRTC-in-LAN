#!/bin/execlineb -P

background {
	caddy run
}
background {
	loopwhilex
		gst-launch-1.0 -m
			whipserversrc stun-server=stun:// signaller::host-addr=http://localhost:8080
			: queue2 : decodebin3 : autoaudiosink
}
wait
